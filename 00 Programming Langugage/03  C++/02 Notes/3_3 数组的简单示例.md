---
title: 3_3 数组的简单示例
aliases:
  - 数组示例
tags:
  - 课程/C++
  - C++/数组
chapter: 3
section: 3_3
course: C++ 程序设计
type: note
status: unfinished
created: 2026-03-17
updated: 2026-03-17
parent_moc: "[[C++ 程序设计 MOC]]"
chapter_note: "[[3_第 3 章 指针、数组、字符串与函数基础]]"
prerequisites: []
related: []
review_level: 0
---

# 3_3 数组的简单示例

## 所属导航
- 总目录：[[C++ 程序设计 MOC]]
- 章节：[[3_第 3 章 指针、数组、字符串与函数基础]]

## 前置知识
-

## 相关知识
-

---

## 核心概念
- 数组名代表数组首元素的地址，但实际输出时需注意值和地址的区别
- 数组元素个数可通过sizeof计算：`sizeof(a) / sizeof(a[0])`
- 数组越界访问可能导致未定义行为或程序崩溃
- 指针大小与指向类型无关（32位系统4字节，64位系统8字节）

## 语法定义
```cpp
// 数组初始化
int a[5] = {0};  // 全部初始化为0
int a[5] = {1, 2, 3};  // 部分初始化，剩余自动为0

// 计算数组元素个数
int n = sizeof(a) / sizeof(a[0]);

// 变量初始化（C++风格）
int b(5);  // 等价于 int b = 5;
```

## 示例代码

```cpp
#include <iostream>
using namespace std;

int main() {
    // 示例1：数组初始化和地址输出
    int a[5] = {0};  // 全部初始化为0
    cout << "a[0] = " << a[0] << endl;

    int b(5);  // C++风格初始化，b = 5
    cout << "b = " << b << endl;

    // 打印数组地址（16进制）
    cout << "数组地址: " << a << endl;

    // 示例2：计算数组元素个数
    int n = sizeof(a) / sizeof(a[0]);
    cout << "数组元素个数: " << n << endl;
    // sizeof(a) = 20字节（5个int）
    // sizeof(a[0]) = 4字节（1个int）
    // n = 20 / 4 = 5

    // 示例3：数组越界访问
    cout << "a[5] = " << a[5] << endl;  // 越界！可能输出随机值
    // a[10000] 将导致程序崩溃（访问冲突 0xC0000005）

    return 0;
}
```

## 运行结果 / 现象
- 初始化后的数组元素值为0
- 地址以16进制显示（32位系统8位16进制，64位系统16位16进制）
- 数组越界访问可能不报错（小范围越界）或导致崩溃（大范围越界）

## 原理理解
- 16进制数特性：每位包含16种可能（0-9，A-F）
- 与二进制的对应关系：1位16进制数 = 4位二进制数（2^4=16）
- 地址长度计算：32位系统地址为8位16进制（32位二进制），64位系统为16位16进制（64位二进制）
- 数组名特殊处理：虽然数组底层通过指针实现，但`sizeof(a)`返回数组总大小而非指针大小
- 内存管理原理：程序运行时获得大块预留空间，数组仅占用其中部分区域
- 越界访问机制：小范围越界可能在预分配内存空间内，大范围越界触发访问冲突

## 易错点
- 数组名在sizeof中表现特殊：返回数组总大小而非指针大小
- 指针变量本身的大小是固定的，与指向类型无关
- 数组越界访问即使不报语法错误也是非法行为
- 64位系统下地址长度是32位系统的2倍

## 面试 / 考试常见问法
- 如何计算数组的元素个数？
- `sizeof(数组名)`和`sizeof(指针)`的区别？
- 32位和64位系统的地址长度分别是多少？
- 数组越界访问为什么会报错或不报错？

## 一句话总结
- 数组大小用sizeof计算，数组名在sizeof中特殊处理，地址用16进制表示，越界访问风险高且行为不确定

---

## 复习

* 掌握程度：
* 是否需要重看视频：
* 下次复习时间：

## 反向链接关注点

> 以后可从这些主题回链到本笔记

*



-- 以下为AI生成的文稿笔记内容 --

### 一、例题:数组输出内容

﻿

00:04﻿

- 数组名a的本质：a代表数组首元素的地址，但实际输出的是首元素的值
- a[0]的赋值：若初始化时未显式赋值，元素值为随机值；显式赋零后a[0]=0
- int b(5)的语义：此为C++面向对象编程中的变量初始化语法，b=5
- 数组初始化验证：通过打印验证数组元素全为0，地址以16进制显示

### 二、16进制数介绍

﻿

05:54﻿

- 16进制数特性：每位包含16种可能（0-9，A-F）
- 与二进制的对应关系：1位16进制数=4位二进制数（2^4=16）
- 地址长度计算：8位16进制地址对应32位二进制地址（8×4=32位）

### 三、64位系统地址长度

﻿

06:56﻿

#### 1.64位系统下地址长度的变化

﻿

07:00

﻿

- 地址长度对比：32位系统地址为8位16进制数，64位系统扩展为16位16进制数
- 实际验证：修改编译环境后，观察到地址长度显著增加

#### 2.数组元素个数的计算方法

﻿

07:51

﻿

|   |   |   |
|---|---|---|
|计算步骤|具体操作|结果|
|获取数组总字节数|sizeof(a)|20字节（5×4）|
|获取单个元素字节数|sizeof(a[0])|4字节（int）|
|计算元素数量|sizeof(a)/sizeof(a[0])|5个元素|

#### 3.例题1:数组与指针的歧义问题

﻿

09:35

﻿

- 指针大小固定性：指针变量本身恒占4字节（32位系统），与指向类型无关
- 数组名特殊处理：虽然数组底层通过指针实现，但sizeof(a)仍返回数组总大小而非指针大小
- 类型作用范围：指针类型仅决定其解引用时访问的内存空间大小

### 四、例题:数组越界访问

﻿

12:30﻿

- 越界访问机制：
    - 小范围越界：操作系统预分配内存空间内访问可能不报错
    - 大范围越界：触发访问冲突（如0xC0000005错误）
- 内存管理原理：程序运行时获大块预留空间，数组仅占用其中部分区域
- 实际验证：a[5]可能输出随机值，而a[10000]必然导致程序崩溃

### 五、32位系统地址长度

﻿

17:03﻿

将系统从64位改为32位（x86架构）后，内存地址长度会显著缩短。访问数组越界时，系统可能报错也可能不报错，但任何超出数组范围的访问操作均属于非法行为，存在潜在错误风险。